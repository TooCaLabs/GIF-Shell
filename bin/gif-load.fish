# Legacy note: GIF is no longer loaded as a fish conf.d plugin.
# The shell entrypoint is `bin/gif`. This file remains only as a
# reference for older checkouts; install.sh does not copy it into
# ~/.config/fish anymore.

echo "gif-load: deprecated — use the GIF shell: gif" >&2
